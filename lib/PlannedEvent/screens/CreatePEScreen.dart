import 'package:flutter/material.dart';

class CreatePEScreen extends StatelessWidget {
  const CreatePEScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Create PE')),
      body: const Center(child: Text('Create PE - stub')),
    );
  }
}
