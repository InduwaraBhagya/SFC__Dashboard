import 'package:flutter/material.dart';

class PEDetailsSearchScreen extends StatelessWidget {
  const PEDetailsSearchScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('PE Details')),
      body: const Center(child: Text('PE Details Search - stub')),
    );
  }
}
