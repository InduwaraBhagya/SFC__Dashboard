import 'package:flutter/material.dart';

class PEReportScreen extends StatelessWidget {
  const PEReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('PE Reports')),
      body: const Center(child: Text('PE Reports - stub')),
    );
  }
}
